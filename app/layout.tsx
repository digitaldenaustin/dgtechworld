import type { Metadata } from "next";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

export const metadata: Metadata = {
  title: "Digital Den Austin | Tested Used Electronics",
  description: "Shop tested pre-owned phones, gaming systems, cameras and electronics for local pickup in North Austin.",
  metadataBase: new URL("https://dgtechworld.com")
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
